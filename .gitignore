node_modules/
dist/
web/sessions/
web/public/shares/

# Claude Code 로컬 설정 (개인/머신 전용)
.claude/

# 원본(대용량) 배너 소스 이미지 — 최적화본 web/public/hero.jpg 만 커밋
Gemini_Generated_Image_*.png

# wrangler / worker
.wrangler/
.dev.vars
node_modules/

# 런타임 생성 설정 파일 (Default 정보관리)
web/settings.json

# 로컬 환경변수 (시크릿 포함) — 절대 커밋 금지. 예시는 web/.env.example 참고
.env
*.env
!.env.example
!*.env.example
